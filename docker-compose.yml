version: "3.9"

services:
  recon:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PHONEINFOGA_URL=http://localhost:5100
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
